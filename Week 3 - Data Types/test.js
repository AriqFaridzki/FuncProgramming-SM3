const numToString = (num, nums) => (num + nums);
const split = (num, separator) => num.split(separator)
const filter = fn => arr => arr.filter(fn)

const mathMsg = (mathOp, x, y) =>{
    const results = mathOp(x,y)
}

console.log(split("asdqas as asda asdasdasda aasda", " "))